我想做一个基于Polymarket（以下简称pm）的多策略交易系统，包括真实交易和模拟交易，先帮我搭建系统框架，再根据我的提示一步一步实现具体功能：
# 1.基础环境：
## 是我基于figma设计的前端页面，理解里面的内容
## 是polymarket的文档，拉取下来放在polyapi文件夹里，便于你以后查阅
## 是goalserve的文档，这是我要接入的外部数据源，拉取下来放在gsapi文件夹里，便于你以后查询
## 本机已安装postgresql+timescale，用户名：postgre，密码：dT3Hu89envHg3Nc，端口5432，polypdt是模式名，在此下建表
## 本机已安装redis，端口6379，无验证

# 2.系统概要：做一个基于polymarket的多策略交易系统，系统可接入外部数据源，可独立开发多种策略，先通过模拟交易验证哪些策略可行，再进行真实交易，现阶段只做足球和篮球的交易

# 3.基于已设计页面的功能描述：
## 左侧比赛区
### 分为进行中live的比赛和未开始pre的比赛
### 按照配置每隔X分钟从pm采集总成交量大于$xxxx K 的某些比赛数据，未来比赛仅采集未来24小时内的
### 接入pm的websocket，针对live的比赛，实时更新数据，包括orderbook、volume、outcomeprices以及比赛的比分、比赛场次和时间等
### 比赛卡片信息中左下角是moneyline volume和总volume，右下角是数据最新刷新时间
## 中间赛事详情区
### 可查看比赛的详细信息，包括比赛场次、时间、队伍、outcomeprices、成交量等
### 图表显示moneyline的outcomeprices的变化，使用echarts库
### 在图表区下方，新添加从外部数据源获取的比赛信息块，显示包括数据更新时间、比分、阵容、近期战绩、赔率等
### 成交记录区，显示的是该比赛的所有成交记录
### 日志区，显示当前比赛的日志，主要显示策略的信息、交易（包括下单、取消、成交等）、错误、警告等
## 右侧交易区：
### 真实交易，在创建真实交易时，可按别名选择配置文件中配置的PM账户，配置包括PM的账户别名、Builder address、 API Key、 Secret、 Passphrase等，显示持仓资金、可用资金等信息，也需要选择策略和配置策略参数
### 模拟交易，在创建模拟交易时，需要选择策略和配置策略参数

# 4.外部数据源：
## 可接入外部数据源，现在可接入的是goalserve，apikey:3cccbf6d8be44f8c3b7a08de721809f7
## 接入外部数据源的websocket，为正在进行的live比赛提供实时的比赛数据
## 接入其常规http请求接口，下载历史比赛数据，包括时间、队伍、阵容、赔率等，存储到数据库中

# 5.开发要求：
## 每个策略为一个或一套独立的文件，以方便维护，或者后期添加其他策略
## 由于比赛的标识可能有一定出入，包括队伍名字可能全称或缩写的不同、比赛时间可能按不同时区显示等，需要将外部数据源的比赛信息与PM的比赛信息进行匹配，推荐一种匹配方式，是否可用AI来做对应的识别
## 尽量不要修改已有的前端页面，只在必要时添加新的组件或功能
## 系统追求高效，需要都用异步或多线程处理
## 由于需要多个策略运行，后期还需要添加策略，策略内应具备几个能力，获取PM和外部数据源的比赛数据、根据数据进行交易决策、记录交易日志、统计交易结果等
## 为防止频繁写读数据库，WS高频次的数据可以先写在redis中，定时重采样写入库，例如prices会有大量数据，写入数据库仅写入X分钟级
## 为了防止策略运行时对数据库的高并发读写，所有对数据库的读写操作，都应异步处理，推荐使用异步数据库驱动
## 重点考虑设计策略模块，WS收到的数据应立即异步分发给各策略，策略应在独立的线程或进程中运行，互不干扰
## 策略应有开发标准：标注哪些变量需要用户配置，也就是在创建策略交易时选择不同策略，每个策略的配置参数应不同，例如不同的交易阈值、不同的交易时间间隔等
## 策略应有接口标准：可以获取策略的配置，获取比赛的PM的数据，接收WS发来的数据，可以写入交易日志，可以发出交易信号